import React, { Component } from 'react';
import Tabar from '../tabar'

class Sort extends Component {
  render () {
    return (
      <div>
        分类
      </div>
    )
  }
}

export default Tabar(Sort);