import React, { Component } from 'react';
import Tabar from '../tabar'

class My extends Component {
  render () {
    return (
      <div>
        我的
      </div>
    )
  }
}

export default Tabar(My);