import React, { Component } from 'react';
import Tabar from '../tabar'

class Home extends Component {
  render () {
    return (
      <div>
        首页
      </div>
    )
  }
}

export default Tabar(Home);