import React, { Component } from 'react';
import Tabar from '../tabar'

class Shop extends Component {
  render () {
    return (
      <div>
        购物车
      </div>
    )
  }
}

export default Tabar(Shop);